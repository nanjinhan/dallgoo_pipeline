# --- 비밀값 / 개인키 (절대 커밋 금지) ---
.env
nas_*
*.key
*.pem
**/.ssh/

# --- 녹화물 / 로그 (용량·개인정보) ---
recordings/
logs/

# --- VPN 설정(비밀) 제외 ---
*.ovpn
# 참고: go2rtc.exe / ffmpeg.exe 는 "클론 후 바로 실행" 목적으로 저장소에 포함(대용량).

# --- Python ---
__pycache__/
*.pyc
.venv/
venv/
학습정리.md
